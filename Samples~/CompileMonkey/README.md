# Compile Monkey Test Script

This sample contains `CompileMonkeyWarningTest.cs`, which generates an intentional compiler warning to demonstrate Compile Monkey's warning visual overlay and audio chime.

## Usage
1. Import this sample into your project.
2. Unity will compile `CompileMonkeyWarningTest.cs`.
3. Notice the Compile Monkey overlay displaying compilation status and warning feedback.
