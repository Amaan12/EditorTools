# Runtime Components

This folder is reserved for runtime code and components that can be used directly in standalone game builds.
Currently, this package focuses on Editor workflow enhancements, but runtime utilities can be added here as needed.
